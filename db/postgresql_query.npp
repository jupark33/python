- password change
alter user postgres with password '1234'

- latest date
select max(dt_reg)
from tb
where type = 1

- latest date
select
max(timestamp)
from tb
where type = 1

- latest date
SELECT
max(timestamp), price
FROM mcs.t_tr_kb
where n_currency_pair = 0
group by price


